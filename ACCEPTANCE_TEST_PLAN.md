# خطة اختبار القبول التشغيلي — SchoolSystem

## الهدف

تحدد هذه الوثيقة الاختبارات اليدوية المطلوبة لاعتماد النظام على جهاز Windows 10/11 مع Visual Studio 2022 وSQL Server فعلي. نجحت الفحوصات الساكنة وإعادة البناء داخل بيئة المشروع، لكن اختبار Runtime مع قاعدة بيانات خارجية يحتاج إلى تنفيذه على بيئة التشغيل الفعلية.

## المتطلبات السابقة

يجب تثبيت Visual Studio 2022 مع workload **.NET desktop development** وTargeting Pack لـ **.NET Framework 4.7.2**. يجب استعادة حزم NuGet عبر `scripts/Restore-Packages.ps1`، وتجهيز نسخة احتياطية من قاعدة البيانات قبل تطبيق `Databass/SchoolDB.SQL` أو `Databass/Migration_Step1.sql`.

يجب تعديل `App.config` ليتوافق مع اسم خادم SQL Server الفعلي وقاعدة البيانات، ثم التأكد من أن حساب Windows المستخدم يملك صلاحية الاتصال بقاعدة `SchoolDB`.

## حالات الاختبار

| الرقم | المجال | الإجراء | النتيجة المتوقعة |
|---:|---|---|---|
| 1 | البناء | فتح `SchoolSystem.sln` ثم `Restore NuGet Packages` و`Rebuild Solution` | ينجح البناء دون أخطاء Compile |
| 2 | الاتصال | تشغيل التطبيق مع ConnectionString صحيح | تظهر شاشة الدخول دون استثناء غير معالج |
| 3 | دخول صحيح | استخدام حساب فعال بصلاحية مناسبة | الانتقال إلى الواجهة الرئيسية وظهور القوائم المسموحة |
| 4 | دخول خاطئ | إدخال اسم مستخدم أو كلمة مرور خاطئة | رسالة عربية عامة دون SQL أو Stack Trace |
| 5 | حساب معطل | محاولة الدخول بحساب غير فعال | رفض الدخول برسالة واضحة |
| 6 | الصلاحيات | الدخول بأدوار مختلفة | إخفاء القوائم والعمليات غير المسموحة وعدم السماح بالوصول المباشر |
| 7 | المستخدمون | إنشاء مستخدم وتعديله وتعطيله ثم إعادة تحميل القائمة | حفظ صحيح وتحديث الجدول وتطبيق حالة الحساب |
| 8 | الطلاب | إضافة طالب وتعديله وحذفه والبحث عنه | تحقق الحقول، تحديث الجدول، ورسائل نجاح آمنة |
| 9 | التسجيل | تسجيل طالب في فصل والتحقق من التكرار | إنشاء التسجيل مرة واحدة ورفض التكرار برسالة واضحة |
| 10 | المعلمون والموظفون | إضافة وتعديل سجل ثم إعادة التحميل | حفظ صحيح وعدم فقدان البيانات |
| 11 | الحضور | تسجيل حضور وغياب وتعديل حالة | حفظ الحالة وعرض العدادات الصحيحة |
| 12 | الرواتب | اختيار موظف وفترة ثم حساب الراتب وحفظه | حساب صحيح حسب قواعد النظام وعدم قبول بيانات ناقصة |
| 13 | المواد والفصول | إضافة مادة وفصل وتعديلهما | تحقق الحقول وتحديث الجداول دون أخطاء |
| 14 | الجدول الدراسي | إنشاء حصة وتعديلها والتحقق من التعارض | رفض التعارض أو عرضه بوضوح حسب قواعد النظام |
| 15 | الدرجات | تحميل الطلاب وإدخال درجات صحيحة وغير صحيحة | قبول القيم الصحيحة ورفض خارج النطاق برسالة عربية |
| 16 | الرسوم والسندات | إنشاء خطة أو رسم أو سند وتعديل الحالة | حفظ مالي واضح وعدم قبول قيم غير صالحة |
| 17 | المصروفات | إدخال مصروف وتعديله والبحث عنه | حفظ وتصفية صحيحة مع تنسيق العملة |
| 18 | المكتبة | إضافة كتاب وإعارة وإرجاع | تحديث حالة الكتاب وحساب التأخير عند الحاجة |
| 19 | النقل | إضافة حافلة ومسار وربط طالب | حفظ البيانات وظهور الجداول دون تعارض |
| 20 | التقارير | تحميل تقرير وتصفية البيانات | ظهور النتائج دون تجميد الواجهة |
| 21 | التصدير | تصدير تقرير إلى Excel وCSV وPDF | إنشاء الملفات وفتحها أو حفظها بالمحتوى الصحيح |
| 22 | الطباعة | معاينة وطباعة تقرير وبطاقة طالب | ظهور RTL صحيح وعدم تسريب موارد أو استثناءات |
| 23 | النسخ الاحتياطي | إنشاء نسخة احتياطية ثم استعادة نسخة اختبارية | نجاح العملية وظهور رسالة النتيجة بوضوح |
| 24 | كلمة المرور | تغيير كلمة المرور ثم تسجيل الدخول بها | قبول الجديدة ورفض القديمة |
| 25 | الإغلاق | إغلاق النوافذ وتسجيل الخروج وإعادة التشغيل | تحرير الموارد وعدم بقاء عمليات معلقة |

## معيار القبول

يُعد النظام جاهزًا للاعتماد التشغيلي عندما تنجح الحالات من 1 إلى 25 على قاعدة بيانات اختبارية، وتُوثق أي حالة فاشلة مع الخطأ والخطوات والبيانات المستخدمة. لا يجوز اختبار الحذف أو الاستعادة على قاعدة الإنتاج دون نسخة احتياطية مؤكدة.

## نتيجة الفحوصات الآلية الحالية

```text
Branch: main
Working tree: clean before documentation
Forms: 22
ApplyStyle missing: NONE
Placeholders: 0
Raw exception UI scan: 0
Direct SQL UI scan: 0
Build: succeeded
Compile errors: 0
git diff --check: PASS
```

## حدود البيئة الحالية

لا تتوفر في بيئة التدقيق الحالية أداة `sqlcmd` ولا جلسة SQL Server Windows متصلة باسم الخادم الموجود في `App.config`، لذلك لا يمكن اعتبار حالات الاتصال وCRUD والطباعة الفعلية ناجحة اعتمادًا على البناء وحده. يجب تنفيذها على جهاز Windows الخاص بالتشغيل.

## أوامر التشغيل

```powershell
cd D:\_Getintopc.com_VS2022_2_2\SchoolSystem
powershell -ExecutionPolicy Bypass -File .\scripts\Restore-Packages.ps1
git switch main
git pull --ff-only origin main
```

بعد ضبط `App.config`، افتح `SchoolSystem.sln` في Visual Studio 2022 ونفذ `Build > Rebuild Solution` ثم `F5`.

**المؤلف:** Manus AI  
**التاريخ:** 2026-08-14
