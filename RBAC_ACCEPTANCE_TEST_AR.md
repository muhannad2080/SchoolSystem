# خطة اختبار Authentication وRBAC

## الهدف

التحقق من أن الصلاحيات تُحمّل من الحساب الصحيح، وأن تسجيل الخروج يمسح الجلسة السابقة، وأن طبقة الخدمات تمنع العمليات غير المصرح بها حتى عند تجاوز عناصر الواجهة.

> يجب تنفيذ اختبارات SQL Server وWinForms على جهاز Windows الذي يحتوي على قاعدة `SchoolDB`. نجاح البناء في بيئة Mono لا يغني عن الاختبار التشغيلي على البيئة الفعلية.

## المتطلبات قبل الاختبار

يجب أخذ نسخة احتياطية من قاعدة البيانات، وتشغيل `Databass/Migration_Step1.sql` عند الحاجة، ثم تشغيل `Databass/Migration_RBAC_Hardening.sql` مرة واحدة من SQL Server Management Studio. يجب إنشاء حسابين تجريبيين: حساب مدير نظام، وحساب مستخدم عادي بدور محدود مثل `التقارير` أو `المكتبة`، مع التأكد من أن الحسابين فعالان وكلمات المرور غير متطابقة.

## تسلسل الاختبار الأساسي

| # | الإجراء | النتيجة المتوقعة |
|---:|---|---|
| 1 | تشغيل التطبيق من شاشة الدخول | لا تظهر أي قائمة تشغيل قبل نجاح المصادقة. |
| 2 | إدخال كلمة مرور خاطئة للمدير | تظهر رسالة عامة ولا تُنشأ جلسة. |
| 3 | تسجيل الدخول بحساب المدير | تظهر كل القوائم والصلاحيات الموجودة في `PermissionKeys.All`. |
| 4 | فتح شاشة إدارة المستخدمين | تنجح العملية ويظهر المستخدمون. |
| 5 | فتح شاشة الدرجات والتقارير والمالية والمكتبة والنقل | تنجح جميع العمليات المسموحة للمدير. |
| 6 | تسجيل الخروج بالموافقة | تُمسح الجلسة وتختفي النافذة الرئيسية القديمة. |
| 7 | تسجيل الدخول بالحساب المحدود | تُعاد تهيئة نافذة رئيسية جديدة بصلاحيات الحساب المحدود فقط. |
| 8 | محاولة فتح شاشة إدارة المستخدمين من القائمة | لا تظهر القائمة، وإذا استُدعيت مباشرة تُرفض بـ`UnauthorizedAccessException`. |
| 9 | محاولة تعديل بيانات خارج دور المستخدم المحدود | تُرفض العملية من طبقة الخدمة، وليس فقط من الواجهة. |
| 10 | فتح شاشة التقارير أو المكتبة حسب الدور | ينجح فقط إذا كانت الصلاحية المقابلة موجودة. |
| 11 | تسجيل الخروج ثم إلغاء تسجيل الدخول | ينتهي التطبيق ولا تبقى جلسة صالحة في الذاكرة. |
| 12 | إعادة تشغيل التطبيق | لا تظهر صلاحيات الحساب السابق؛ يجب تسجيل الدخول من جديد. |

## اختبار المدير والصلاحيات

بعد تشغيل migration، يجب تنفيذ الاستعلام الآتي والتحقق من أن كل حساب يحمل الدور `مدير النظام` لديه القائمة الكاملة نفسها، بما فيها `Users.Manage` و`Reports.View` و`Library.Manage` و`Transport.Manage`:

```sql
SELECT UserID, UserName, RoleName, Permissions, IsActive
FROM dbo.Users
WHERE LOWER(LTRIM(RTRIM(ISNULL(RoleName, N'')))) = LOWER(N'مدير النظام');
```

يجب إعادة تشغيل migration مرة ثانية. النتيجة المتوقعة هي عدم حدوث تغيير وظيفي أو إنشاء صفوف مكررة، وهو معيار idempotency.

## اختبار عزل الجلسة

يجب تنفيذ التسلسل التالي دون إغلاق التطبيق يدويًا: الدخول كمدير، فتح إدارة المستخدمين، تسجيل الخروج، الدخول كمستخدم محدود، ثم محاولة تنفيذ عملية إضافة مستخدم أو حذف مستخدم أو فتح شاشة لا يملكها. يجب رفض العملية. بعد ذلك يجب تسجيل الخروج وإغلاق التطبيق، ثم إعادة تشغيله والدخول بالحساب المحدود مرة أخرى؛ يجب ألا تظهر أي قائمة أو صلاحية من جلسة المدير السابقة.

## اختبار حماية الخدمة

يمكن اعتبار الاختبار ناجحًا إذا تحققت الشروط الآتية في الكود والتشغيل: كل خدمة عامة تتعامل مع بيانات حساسة تستدعي `CurrentUser.DemandPermission` أو `CurrentUser.DemandAny`، وعمليات إدارة المستخدمين تستدعي `Users.Manage`، ودور مدير النظام يعتمد على القائمة المركزية، ولا توجد مقارنة أمنية تعتمد على اسم المستخدم، و`MainForm` يمسح الجلسة عند الإغلاق المباشر أو عند إلغاء تسجيل الدخول.

## تسجيل النتائج

يُسجل لكل حالة رقم الاختبار، واسم الحساب، والدور، والصلاحيات، والنتيجة الفعلية، ورسالة الخطأ إن وجدت. أي فشل في عزل الجلسة أو السماح بعملية محمية يُعامل كفشل أمني عالي الأولوية ولا يُعتمد الإصدار قبله.
