# دليل التشغيل والإدارة اليومية — SchoolSystem

## الغرض

يوضح هذا الدليل طريقة تجهيز النظام وتشغيله ومراقبته ونسخ بياناته احتياطيًا واستعادتها. النظام تطبيق Windows Forms يستهدف .NET Framework 4.7.2 ويعتمد على SQL Server؛ لذلك يجب تنفيذ خطوات التحقق على جهاز Windows متصل بقاعدة البيانات الفعلية قبل اعتماد الإصدار للاستخدام اليومي.

## متطلبات الاعتماد

يجب توفير جهاز تشغيل Windows محدث، و.NET Framework 4.7.2 أو إصدار أحدث متوافق، واتصال شبكي ثابت بخادم SQL Server، وحساب Windows أو SQL مخصص للتطبيق. يجب إنشاء قاعدة `SchoolDB` وفق المخطط الذي يعتمد عليه المشروع، ومنع استخدام حساب `sa` أو حساب ذي صلاحيات إدارية واسعة من ملف إعداد التطبيق.

| العنصر | الإجراء المطلوب قبل التشغيل |
|---|---|
| ملف الإعداد | نسخ `App.config.example` إلى إعداد الإصدار وتعديل `Server` و`Database` فقط حسب البيئة |
| قاعدة البيانات | تطبيق المخطط والبيانات المرجعية على قاعدة تجريبية أولًا |
| الصلاحيات | إنشاء مدير نظام أولي ثم توزيع الصلاحيات حسب الدور الوظيفي |
| السجلات | التأكد من قدرة حساب المستخدم على إنشاء `%LocalAppData%\\SchoolSystem\\Logs` |
| النسخ الاحتياطي | تنفيذ نسخة كاملة قبل أول استخدام فعلي وتسجيل موقعها وتاريخها |
| الاختبار | تنفيذ سيناريوهات تسجيل الدخول والطلاب والمعلمين والرسوم والتقارير على بيانات تجريبية |

## التشغيل اليومي

يجب تشغيل التطبيق من نسخة إصدار ثابتة، مع الاحتفاظ برقم الإصدار وتاريخ البناء في سجل المدرسة. عند ظهور رسالة خطأ عامة، لا تُعاد محاولة العملية الحساسة بشكل متكرر قبل التحقق من الاتصال أو البيانات. تُراجع ملفات السجل المحلية من مسؤول النظام فقط، ولا تُرسل إلى مستخدمين غير مخولين لأنها قد تحتوي على تفاصيل تشخيصية.

يجب تسجيل عمليات النسخ الاحتياطي في سجل بسيط يتضمن اسم القاعدة، تاريخ التنفيذ، اسم ملف النسخة، حجم الملف، ونتيجة التحقق. لا يكفي نجاح أمر النسخ؛ يجب اختبار الاستعادة على قاعدة منفصلة بصورة دورية.

## النسخ الاحتياطي

ينفذ السكربت `scripts/Backup-SchoolDB.ps1` نسخة كاملة باستخدام `BACKUP DATABASE` إلى مجلد يحدده مسؤول قاعدة البيانات. يجب حفظ النسخ على قرص مختلف أو مساحة شبكية محمية، وعدم حفظها داخل مجلد البرنامج أو مستودع Git. يوصى بالاحتفاظ بعدة نسخ يومية وأسبوعية وفق سياسة المدرسة، مع تقييد الوصول إلى ملفات `.bak`.

قبل تشغيل السكربت، يجب فتح PowerShell بصلاحيات مناسبة وتعديل المتغيرات الموجودة في بدايته. عند استخدام Windows Authentication يترك `SqlCredential` فارغًا. عند استخدام حساب SQL، يجب تمرير بيانات الاعتماد من مدير الأسرار أو من جلسة PowerShell آمنة، وليس كتابتها داخل Git.

## الاستعادة

لا تُستعاد نسخة فوق قاعدة الإنتاج مباشرة قبل أخذ نسخة إضافية من حالتها الحالية. يُفضّل أولًا استعادة الملف إلى قاعدة اختبار باسم مختلف، وفحص الجداول وعدد السجلات وفتح التطبيق عليها. بعد موافقة مسؤول النظام، تُنفذ الاستعادة في نافذة صيانة مع منع الاتصالات النشطة وتوثيق وقت البدء والانتهاء والنسخة المستخدمة.

## خطة التعامل مع الأعطال

عند فشل الاتصال، يُفحص اسم الخادم والمنفذ، ثم صلاحية الحساب واتصال الشبكة، ثم حالة خدمة SQL Server. عند فشل عملية بيانات، يُحتفظ برسالة المستخدم العامة ورقم الوقت التقريبي، ثم تُراجع السجلات المحلية دون عرض تفاصيلها للمستخدمين. عند الاشتباه في تلف البيانات، يوقف الإدخال ويُحفظ آخر ملف نسخة احتياطية سليم قبل أي محاولة إصلاح.

> لا يمكن إعلان الجاهزية الإنتاجية الكاملة من بيئة Linux وحدها؛ يجب تنفيذ اختبار قبول فعلي على Windows وSQL Server، وتجربة استعادة نسخة احتياطية، وفحص التقارير والطباعة والتصدير على جهاز المدرسة.

## قائمة قبول الإصدار

| البند | النتيجة | ملاحظات |
|---|---|---|
| تسجيل الدخول والخروج | ☐ | |
| الصلاحيات حسب الأدوار | ☐ | |
| إدارة الطلاب والمعلمين | ☐ | |
| الحضور والدرجات والجدول | ☐ | |
| الرسوم والمصروفات والسندات والرواتب | ☐ | |
| التقارير والطباعة والتصدير | ☐ | |
| النسخ الاحتياطي والاستعادة التجريبية | ☐ | |
| الاتصال من أجهزة المستخدمين | ☐ | |
| مراجعة السجلات وحماية ملفاتها | ☐ | |
| اعتماد مسؤول النظام | ☐ | |
