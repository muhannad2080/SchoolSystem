# توثيق نظام إدارة المدرسة الشامل (School Management System Documentation)

هذا المستند يمثل التوثيق الهندسي والفني الشامل لنظام إدارة المدرسة (SchoolSystem)، مصمم خصيصاً ليسلمه المطور لأي فريق أو مطور جديد ليبدأ العمل والتطوير على النظام فوراً وبفهم كامل لمعمارية المشروع، طبقاته، آلية الاتصال بقاعدة البيانات، واجهات المستخدم، ومعايير الجودة والأمان المطبقة.

---

## 1. التعريف بالمشروع ونظرة عامة

* **عنوان المشروع:** نظام إدارة المدرسة المتكامل (School Management System)
* **نوع التطبيق:** تطبيق سطح مكتب مكتوب بلغة C# باستخدام تقنية **Windows Forms (.NET Framework 4.7.2)**.
* **قطاع الأعمال:** قطاع التعليم المدرسي والتربوي.
* **الهدف الرئيسي:** توفير نظام متكامل ومستقر لإدارة شؤون الطلاب، المعلمين، الحضور والغياب، الرتب المالية والرسوم الدراسية، السندات، والمستودعات والتقارير مع دعم واجهة عربية بالكامل (RTL) ومعمارية برمجية نظيفة.

---

## 2. المعمارية البرمجية (System Architecture)

يتبع النظام معمارية متعددة الطبقات (Layered / N-Tier Architecture) لفصل المسؤوليات وتسهيل الصيانة والتطوير:

```
┌───────────────────────────────────────────────┐
│           UI Layer (Windows Forms)            │  <- Forms & UserControls (UIHelper, RTL)
└──────────────────────┬────────────────────────┘
                       │
┌──────────────────────▼────────────────────────┐
│           Services Business Logic             │  <- Business Rules & Validations
└──────────────────────┬────────────────────────┘
                       │
┌──────────────────────▼────────────────────────┐
│         Data Access Layer (Repositories)      │  <- SQL Queries, Parameters, CRUD
└──────────────────────┬────────────────────────┘
                       │
┌──────────────────────▼────────────────────────┐
│            Database (SQL Server)              │  <- Relational DB & Tables
└───────────────────────────────────────────────┘
```

1. **طبقة العرض (UI Layer - `UI/`):** تحتوي على نماذج Windows Forms وعناصر التحكم (UserControls). تدعم تخطيط Right-to-Left (RTL)، وتستخدم `UIHelper` لتوحيد التصميم والمظهر البصري (Design System).
2. **طبقة منطق العمل (Services Layer - `Services/`):** تحتوي على خدمات الأعمال (مثل `StudentService`, `FeeService`, `VoucherService`, `UserService`...) للتحقق من صحة المدخلات وتطبيق القواعد الإدارية والمالية.
3. **طبقة الوصول للبيانات (Data Access Layer - `DataAccess/`):** تحتوي على مستودعات البيانات (Repositories) التي تتولى تنفيذ أوامر SQL باستخدام **ADO.NET** مع معاملات آمنة (SQL Parameters) لمنع حقن SQL.
4. **طبقة النماذج والأمان (`Models/` & `Security/`):** تحتوي على هياكل الكائنات (Entities)، نظام التشفير (`PasswordHasher`)، والصلاحيات (`PermissionKeys` و `CurrentUser`).

---

## 3. تقنيات والمكتبات المستخدمة (Tech Stack & NuGet Packages)

* **اللغة والبيئة:** C# 7.3+, .NET Framework 4.7.2
* **واجهة المستخدم:** Windows Forms (WinForms)
* **قاعدة البيانات:** Microsoft SQL Server
* **مكتبات التصدير والتقارير:**
  * **ClosedXML** (إصدار 0.105.0) لتصدير التقارير وجداول البيانات إلى ملفات Excel منسقة.
  * **iTextSharp.LGPLv2.Core** (إصدار 3.8.1) لتوليد وتقارير وتصدير ملفات PDF.
  * **DocumentFormat.OpenXml** لمعالجة مستندات OpenXML.

---

## 4. آلية الاتصال بقاعدة البيانات (Data Access & Connection)

* **ملف الاتصال (`App.config`):** يحتوي على سلسلة الاتصال (Connection String) بقاعدة البيانات SQL Server.
* **كلاس الاتصال المركزي (`DbConnection.cs`):** يوفر طريقة موحدة لإنشاء وتوسيط اتصالات `SqlConnection` داخل كتل `using` مضمونة الإغلاق لتجنب تسرب الذاكرة أو اتصالات قاعدة البيانات المفتوحة.
* **الأمان:** جميع استعلامات SQL تستخدم المعاملات (`SqlCommand.Parameters.AddWithValue`) بشكل صارم لمنع هجمات SQL Injection.

---

## 5. قواعد التحقق (Validation & Error Prevention)

تم تطبيق نظام التحقق الشامل (Validation) في كل من واجهات المستخدم (UI) وطبقة الخدمات (Services):
* **حقول الأسماء:** تمنع إدخال الأرقام أو الرموز غير المناسبة (عبر تعبيرات منتظمة `Regex` و `KeyPress`).
* **حقول الأرقام والهواتف:** تقبل الأرقام فقط وتتحقق من صحة أطوال أرقام الهواتف (7 إلى 15 رقم).
* **التواريخ:** التحقق من منطقية التواريخ (مثل منع إدخال تاريخ ميلاد في المستقبل).
* **المبالغ والدرجات:** منع القيم السالبة والتحقق من عدم تجاوز الدرجة النهائية.
* **منع التكرار:** التحقق من عدم تكرار أرقام الهوية، أرقام الطلاب، أو أسماء المستخدمين.

---

## 6. دليل المطورين للعمل على النظام (Developer Quick Start)

إذا أردت العمل على هذا المشروع محلياً، اتبع الخطوات التالية:

1. **سحب آخر التحديثات من الفرع المعتمد:**
   ```bash
   git fetch origin
   git checkout improve-ui-usability-stability
   git pull origin improve-ui-usability-stability
   ```
2. **استعادة حزم NuGet:**
   تأكد من استعادة الحزم عبر Visual Studio أو باستخدام `nuget.exe restore SchoolSystem.sln`.
3. **فتح المشروع:**
   افتح ملف `SchoolSystem.sln` في Visual Studio (2019 أو أحدث).
4. **بناء وتشغيل النظام:**
   اضغط `Ctrl + Shift + B` للبناء (Build Solution)، ثم `F5` للتشغيل.

---

## 7. ملخص التعديلات الأخيرة في فرع `improve-ui-usability-stability`

* إصلاح أخطاء البناء (Build Errors) السابقة المتعلقة بملفات الـ Target ومراجع .NET Standard.
* تصحيح استدعاء واجهة الطلاب (`StudentsForm`) في شاشة الترحيب (`WelcomeScreen`) لتفتح بشكل صحيح عبر `LoadFormInPanel`.
* توحيد استخدام الكلاس `UIHelper` في جميع الواجهات لضمان تناسق التصميم (RTL، الألوان، الخطوط).
* إضافة Validation شامل على حقول إدخال بيانات الطلاب والرسوم.
* اختبار واستقرار جميع أزرار النظام والـ DataGridViews.
