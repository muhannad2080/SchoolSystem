# دليل تشغيل SchoolSystem في بيئة فعلية

## المتطلبات

يحتاج النظام إلى Windows مع .NET Framework 4.7.2 أو أحدث، وSQL Server بصلاحية اتصال من جهاز التشغيل، إضافة إلى قاعدة بيانات `SchoolDB` بالمخطط والجداول المتوقعة من المشروع.

## إعداد قاعدة البيانات

بعد بناء التطبيق، يوجد ملف إعداد بجانب الملف التنفيذي باسم `SchoolSystem.exe.config`. يجب ضبط قسم `connectionStrings` قبل أول تشغيل:

```xml
<connectionStrings>
  <add name="SchoolDBConnection"
       connectionString="Server=SERVER_NAME;Database=SchoolDB;Integrated Security=True;TrustServerCertificate=True;"
       providerName="System.Data.SqlClient" />
</connectionStrings>
```

يمكن استخدام حساب Windows مع `Integrated Security=True`، أو حساب SQL مخصص للتطبيق عند الحاجة. لا يُنصح بوضع كلمة مرور في مستودع GitHub أو داخل ملفات المصدر. إذا استُخدم حساب SQL، يجب تقييد صلاحياته على قاعدة `SchoolDB` فقط وتخزين ملف الإعداد في جهاز التشغيل بصلاحيات وصول مناسبة.

## أول تشغيل

شغّل التطبيق بحساب مدير نظام مُنشأ في قاعدة البيانات، ثم أنشئ المستخدمين والصلاحيات من شاشة إدارة المستخدمين. اختبر تسجيل الدخول، الطلاب، المعلمين، الحضور، الرسوم، السندات، المصروفات، الرواتب، التقارير، والنسخ الاحتياطي قبل إدخال بيانات فعلية.

## النسخ الاحتياطي

يجب تنفيذ نسخة احتياطية دورية من قاعدة `SchoolDB` على وسيط منفصل عن جهاز قاعدة البيانات، مع اختبار استعادة نسخة بصورة دورية. لا تعتبر ملفات البرنامج وحدها نسخة احتياطية؛ البيانات التشغيلية موجودة في SQL Server.

## سجل الأخطاء

يسجل التطبيق الأخطاء غير المتوقعة في المسار التالي:

`%LocalAppData%\\SchoolSystem\\Logs\\application-errors.log`

وتُسجل أخطاء الواجهات التي تستخدم `UIHelper` في:

`%LocalAppData%\\SchoolSystem\\Logs\\errors.log`

يجب حماية هذه الملفات من الوصول غير المصرح به، ومراجعتها عند ظهور رسالة خطأ عامة للمستخدم.

## التحقق قبل الإنتاج

قبل اعتماد النظام، يجب تنفيذ اختبار قبول وظيفي على نسخة تجريبية، والتحقق من الاتصال من جهاز كل مستخدم، وصلاحيات مجلد التطبيق، وصلاحيات قاعدة البيانات، وسلامة التقارير والتصدير، ثم توثيق اسم إصدار البرنامج وتاريخ آخر نسخة احتياطية.

> هذه الوثيقة توضح متطلبات التشغيل والإعداد. اعتماد النظام للإنتاج يتطلب اختبار بيئة Windows وSQL Server الفعلية الخاصة بالمدرسة، لأن بيئة التطوير الحالية لا تحتوي على SQL Server Windows فعلي.
