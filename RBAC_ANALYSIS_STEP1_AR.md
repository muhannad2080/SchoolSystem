# تحليل أولي لنظام Authentication وAuthorization وRBAC

## نطاق الفحص
تم فحص `Models/User.cs` و`Security/CurrentUser.cs` و`Security/PermissionKeys.cs` و`Security/PasswordHasher.cs` و`DataAccess/UserRepository.cs` و`Services/UserService.cs` و`UI/LoginForm.cs` و`MainForm.cs` و`UI/UsersForm.cs` وملفات SQL الخاصة بالمستخدمين.

## الملاحظات المثبتة من الكود

1. لا توجد نماذج `Role` أو `Permission` مستقلة، ولا تظهر جداول `Roles` أو `Permissions` أو `UserRoles` أو `RolePermissions` في مخطط SQL المفحوص. النظام الحالي يستخدم `Users.RoleName` و`Users.Permissions` كسلسلة مفاتيح مفصولة بفواصل.

2. `PermissionKeys` هو القاموس المركزي الحالي ويحتوي 20 مفتاحًا فعليًا، بينما `PermissionKeys.GetRoleDefaults` يولد صلاحيات الدور من اسم عربي ثابت.

3. `CurrentUser.HasPermission` يملك تجاوزًا خاصًا: إذا تطابق `RoleName` مع النص العربي `مدير النظام` فإنه يعيد `true` لأي مفتاح. هذا يجعل الدور يتجاوز بيانات الصلاحيات بدل أن يعتمد RBAC على العلاقة المخزنة، وهو مخالف لمبدأ المصدر الواحد للصلاحيات.

4. `UserService.EnsureRolePermissions` يملأ الصلاحيات فقط عندما تكون قيمة `Users.Permissions` فارغة. لذلك الحساب الإداري الذي يملك قائمة ناقصة أو مفاتيح قديمة لن تتم مزامنته إلى قائمة الدور الحالية، وقد لا تظهر له بعض القوائم إذا لم يطابق اسم الدور النص المتوقع.

5. Migration الحالية تطبق نفس السياسة: تحدث حسابات `مدير النظام` فقط عندما تكون `Permissions` فارغة، ولا تطبع أو توحد المفاتيح الموجودة جزئيًا. كما أن هناك نسختين لمسار الترحيل (`Databass/Migration_Step1.sql` و`DatabaseBackup/Migration_Step1.sql`) يجب توحيد التعامل معهما دون حذف البيانات.

6. `UserService.Authenticate` ينفذ `CurrentUser.Set(user)`، ثم تنفذه `LoginForm` مرة ثانية. التكرار غير خطير بحد ذاته لكنه يوزع مسؤولية إنشاء الجلسة بين طبقتين.

7. تسجيل الخروج في `MainForm` ينفذ `CurrentUser.Clear()` ثم يفتح نموذج دخول جديد، وهذا يمسح الكائن الحالي. يلزم اختبار دورة الدخول المتعاقب وإضافة حماية تمنع بقاء حالة قديمة مستقبلًا.

8. خدمات محددة فقط تطبق حماية فعلية على مستوى الخدمة (`StudentsManage`, `ExpensesManage`, `FeesManage`, `PayrollManage`, `TeachersManage`, `TimetableManage`, `VouchersManage`). يلزم جرد بقية الخدمات والواجهات قبل تعديلها، لأن إخفاء القائمة وحده ليس حماية كافية.

9. كلمات المرور لا تحفظ كنص صريح في التدفق الحالي؛ يستخدم `PasswordHasher` مع salt عشوائي وPBKDF2 وحقلي `PasswordHash` و`PasswordSalt`. يلزم الحفاظ على التوافق وعدم تغيير صيغة التخزين دون ضرورة.

## الاستنتاج الأولي
الخلل البنيوي الأوضح ليس نقصًا في عدد مفاتيح `PermissionKeys`، بل أن النظام الحالي يخلط بين دور نصي وصلاحيات منسوخة لكل مستخدم. المزامنة انتقائية (فقط عند الفراغ)، ودور المدير يعتمد على bypass نصي، لذلك يمكن أن تختلف النتيجة بحسب قيمة `RoleName` ومحتوى `Permissions` المخزن. لا يجوز تعديل SQL قبل فحص البيانات الفعلية في قاعدة المستخدم؛ سيتم إعداد استعلام تشخيصي آمن بدل التخمين.

## خطة الخطوة التالية
فحص كامل لمسارات `UserService` و`UserRepository` و`LoginForm` و`MainForm` و`UsersForm`، ثم إعداد استعلامات تشخيصية لمخطط وبيانات Users، وبعدها تحديد Root Cause النهائي قبل أي تعديل تنفيذي.

## نتائج فحص قاعدة البيانات

مخطط `Databass/SchoolDB.SQL` يعرّف جدول `Users` فقط ضمن نظام الدخول، مع `UserID` كمفتاح أساسي وفهرس فريد على `UserName`، وحقلي `RoleName` و`Permissions`. لم يظهر في المخطط أي جدول مستقل للأدوار أو الصلاحيات أو جداول الربط.

ملف `Databass/Migration_Step1.sql` يحتوي تحديثًا idempotent نسبيًا لحقول Users ويملأ صلاحيات الدور عند كونها NULL أو فارغة فقط. توجد نسخة أخرى باسم `DatabaseBackup/Migration_Step1.sql` تختلف عن النسخة الموجودة في `Databass` وتفتقد قسم إصلاح صلاحيات المستخدمين في الجزء المقارن؛ يجب عدم ترك نسختين متناقضتين بعد الإصلاح.

إعداد الاتصال في `App.config` يشير إلى SQL Server خارجي باسم `MUHANNADALJRADI` وقاعدة `SchoolDB`. لا توجد في بيئة التنفيذ الحالية بيانات اتصال فعلية مؤكدة تسمح بإثبات صفوف Users الحية؛ لذلك لن يتم الادعاء بفحص البيانات الفعلية. سيتم إنشاء استعلام تشخيصي آمن يعتمد على SELECT فقط، ويُطلب تشغيله على قاعدة المستخدم لتأكيد الصفوف المتأثرة قبل تطبيق migration.

## Root Cause المرجح المثبت من التصميم

السبب البنيوي المؤكد هو الاعتماد على صلاحيات منسوخة داخل كل مستخدم مع مزامنة عند الفراغ فقط، مع fallback إداري يعتمد على مقارنة اسم عربي ثابت. هذا يسمح بأن تظل صلاحيات Admin جزئية إذا كانت غير فارغة، كما يجعل اختلاف اسم الدور أو وجود مفاتيح قديمة يؤثر مباشرة في التنقل. لا يمكن الجزم بأي صف إداري محدد حتى تُشغّل استعلامات التشخيص على SQL Server الحي.
