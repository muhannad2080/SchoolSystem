# دليل الاختبار التشغيلي لنظام SchoolSystem

## الغرض ونطاق الاختبار

هذا الدليل مخصص لاختبار النسخة الحالية من نظام **SchoolSystem** بعد دفعات التحصين الأخيرة. يجب تنفيذ الاختبارات على جهاز Windows يحتوي على Visual Studio 2022 و.NET Framework 4.7.2 وSQL Server، لأن بيئة التدقيق الحالية تتحقق من المصدر من خلال التدقيق الساكن ولا توفر بناء WinForms فعلياً.

> معيار النجاح: لا يكفي أن تفتح الشاشة؛ يجب أن تنجح العملية الصحيحة، وأن تُرفض العملية غير الصحيحة برسالة عربية واضحة، وأن يظهر الأثر في سجل الأنشطة عندما تكون العملية حساسة.

## أولاً: جلب آخر نسخة وبناء المشروع

افتح PowerShell داخل مجلد المشروع ونفّذ:

```powershell
git switch main
git pull --ff-only origin main
```

تأكد من أن الحالة نظيفة:

```powershell
git status
```

افتح ملف `SchoolSystem.sln` من داخل Visual Studio، وليس ملف الحل كنص. من قائمة **Build** اختر **Rebuild Solution**. إذا ظهرت أخطاء حزم، نفّذ استعادة NuGet من Visual Studio ثم أعد البناء. يجب ألا توجد أخطاء `CS` قبل بدء اختبار التشغيل.

## ثانياً: تحديث قاعدة البيانات بأمان

أنشئ نسخة احتياطية يدوية من قاعدة البيانات الحالية قبل تشغيل أي ترحيل. شغّل ملفات الترحيل الموجودة في مجلد `Databass` بالترتيب الموثق داخل المشروع، ولا تعِد تشغيل ملف إنشاء قاعدة البيانات من الصفر على قاعدة `SchoolDB` المستخدمة فعلياً؛ ذلك قد ينتج أخطاء من نوع وجود قاعدة أو جدول أو فهرس مسبقاً.

بعد الترحيل، تحقّق من وجود الجداول الأساسية التالية:

| المجال | الجداول المطلوبة |
|---|---|
| الطلاب | `Students`, `Classes`, `StudentClasses` |
| الأكاديمي | `Subjects`, `Marks` أو `StudentGrades`, `StudentAttendance` |
| الموظفون | `Teachers`, `TeacherAttendance`, `TeacherContracts` أو جداول الرواتب المعتمدة |
| المالي | `Fees`, `FeePlans`, `Vouchers`, `Expenses` |
| التشغيل | `Books`, `Borrowings`, `Buses`, `BusRoutes` |
| الأمن والتدقيق | `Users`, `AuditLogs` |

تحقّق أيضاً من الأعمدة التي يعتمد عليها النظام، ومنها `Students.StudentPhone` و`Students.Section` و`Students.ClassID`، وعدم استخدام `Phone` للطلاب إلا إذا كان استعلام التوافق يعيد تسميته من `StudentPhone`.

## ثالثاً: اختبار تسجيل الدخول والصلاحيات

أنشئ أو استخدم ثلاثة حسابات اختبار: مدير النظام، مستخدم شؤون الطلاب، ومستخدم مالي. سجّل الدخول بكل حساب على حدة، وأغلق البرنامج بين الاختبارات لضمان إعادة تحميل الصلاحيات من قاعدة البيانات.

| الاختبار | النتيجة المطلوبة |
|---|---|
| المدير | ظهور كل الوحدات الممنوحة له، مع القدرة على فتحها وإدارتها |
| شؤون الطلاب | ظهور الطلاب والتسجيل والفصول الممنوحة فقط، ومنع فتح المالية أو الإعدادات عند عدم منحها |
| المستخدم المالي | ظهور الرسوم والسندات والمصروفات والتقارير المالية فقط بحسب الصلاحيات |
| مستخدم بلا صلاحية | رسالة عربية واضحة عند محاولة الوصول المباشر، وعدم فتح الشاشة |
| تعديل صلاحية مستخدم | بعد تسجيل الخروج والدخول مجدداً، تتغير القوائم والعمليات وفق الصلاحيات الجديدة |

اختبر الفرق بين صلاحية العرض وصلاحية الإدارة: المستخدم الذي يملك العرض يستطيع قراءة البيانات، لكنه لا يستطيع إضافة أو تعديل أو حذف سجل.

## رابعاً: اختبار الطلاب والتسجيل

أضف طالباً باسم رباعي صحيح، وتاريخ ميلاد غير مستقبلي، ورقم هاتف صالح. اختبر إدخال الأرقام العربية مثل `٠٧٧١٢٣٤٥٦٧`، وتأكد من قبولها أو تحويلها وفق السياسة المعتمدة. جرّب رقم هوية أو هاتفاً مكرراً وتأكد من رفضه.

أنشئ طلب تسجيل مع طالب وصف وعام دراسي صحيح مثل `2026/2027`. اختبر رفض التسجيل عند غياب الطالب أو الصف أو عند استخدام عام غير متسلسل. حاول تسجيل الطالب نفسه مرتين في العام نفسه، ويجب رفض العملية الثانية. نفّذ تعديلًا وحذفاً ناجحين، ثم تحقق من ظهور العمليات الثلاث في سجل الأنشطة.

## خامساً: اختبار الدرجات والحضور

في شاشة إدخال الدرجات، اختر طالباً ومادة وفصلاً صحيحاً وأدخل درجات بصيغة عربية أو محلية. اختبر القيم السالبة والقيم التي تتجاوز الحد المسموح، واختبر تكرار نفس الطالب والمادة ونوع الاختبار؛ يجب رفض السجل المكرر أو منعه حسب قواعد الشاشة.

في التحضير اليومي، سجّل حضور طالب ثم حاول تسجيله مرة ثانية في التاريخ نفسه. جرّب كل حالات الحضور المعتمدة، وأدخل ملاحظة طويلة جداً، ومعرف طالب غير صالح. يجب رفض الصف غير الصالح وعدم تنفيذ حفظ جماعي جزئي غير متوقع.

في حضور المعلمين، جرّب تاريخاً أو وقتاً غير صالح ومعرف معلم غير موجود وحالة غير معتمدة. تحقق من أن التعديل والحذف لا يعملان دون اختيار سجل صحيح، وأن العمليات الحساسة تظهر في سجل الأنشطة إذا كانت الخدمة تسجلها.

## سادساً: اختبار المالية والسندات والرواتب

أضف رسماً بقيمة صحيحة، ثم اختبر الخصم الأكبر من الإجمالي، والمدفوع الأكبر من الصافي، والمبالغ السالبة، والأرقام العربية والفواصل المحلية. يجب حساب الصافي والمتبقي والحالة دون استثناءات تحويل.

في شاشة السندات، اختبر سند قبض وسند صرف، ثم جرّب مبلغاً فارغاً أو غير رقمي أو سالب. تحقق من منع الحذف دون اختيار سند، ومن تحديث الجدول بعد الحفظ، ومن تسجيل الإنشاء والتعديل والحذف في سجل الأنشطة.

في الرواتب، اختبر الراتب الأساسي والبدلات بالأرقام العربية، واختر معلماً صحيحاً ثم جرّب الحفظ دون معلم. اختبر تاريخ العقد غير الصحيح وتعارض العقد، وتأكد من رفض البيانات قبل الوصول إلى قاعدة البيانات.

## سابعاً: اختبار المكتبة والنقل

في المكتبة، أضف كتاباً بعدد نسخ صحيح وسنة نشر صحيحة. جرّب عدد نسخ سالباً أو سنة غير منطقية، ثم أنشئ إعارة بكتاب أو مستعير غير صالح. أعد الكتاب وتأكد من عدم نزول عدد النسخ المتاحة عن الصفر.

في النقل، أضف حافلة بسعة ورقم صحيحين، ثم أنشئ مساراً مرتبطاً بحافلة موجودة. جرّب سعراً أو سعة سالبة ومعرف حافلة غير موجود ووقتاً غير صالح. اختبر عدم حذف حافلة مرتبطة بمسار أو عرض رسالة تمنع العملية مع توضيح سببها.

## ثامناً: اختبار التقارير

شغّل تقارير الطلاب والمعلمين والتسجيل والحضور والرواتب، ثم شغّل تقريري الرسوم والدرجات تحديداً. غيّر الفترة الزمنية والعام الدراسي والصف والشعبة والحالة ونص البحث، وتحقق من تغير النتائج فعلياً.

يجب أن تظهر أعمدة عربية واضحة، وألا تظهر أخطاء `Invalid column name`. تحقق من أن تقرير الرسوم يعرض الطالب والصف والإجمالي والمدفوع والمتبقي والحالة، وأن تقرير الدرجات يستخدم جدول `StudentGrades` الحديث عند توفره أو جدول `Marks` القديم كخيار توافق.

## تاسعاً: اختبار سجل الأنشطة

نفّذ إضافة وتعديل وحذف لطالب ومعلم وطلب تسجيل ودرجة ورسم وسند ومصروف ومستخدم. افتح سجل الأنشطة بحساب يملك صلاحية `AuditLogs.View`، واستخدم البحث والفترة الزمنية.

تحقق من ظهور اسم المستخدم أو كلمة `نظام` عند العمليات النظامية، واسم الكيان ومعرفه ونوع العملية والتفاصيل والتاريخ. اختبر مستخدماً لا يملك الصلاحية، ويجب منعه برسالة واضحة.

## عاشراً: اختبار النسخ الاحتياطي والاستعادة

من شاشة الإعدادات، اختر مجلداً موجوداً وقابلاً للكتابة، ثم أنشئ نسخة احتياطية. اختبر أيضاً مجلداً غير موجود؛ يجب إنشاء المجلد تلقائياً أو إظهار رسالة واضحة دون انهيار البرنامج. لا تستخدم مساراً يشير إلى ملف موجود بدلاً من مجلد.

تحقق من وجود ملف `.bak` بحجم غير صفري، وسجّل وقت إنشائه. نفّذ اختبار الاستعادة على قاعدة اختبار منفصلة، وليس على قاعدة الإنتاج، وتأكد من عودة قاعدة البيانات إلى وضع `MULTI_USER` بعد العملية. تحقق من ظهور عمليتي النسخ والاستعادة في سجل الأنشطة.

## معايير القبول النهائية

| المجال | معيار القبول |
|---|---|
| البناء | لا توجد أخطاء C# أو أخطاء Designer |
| الواجهة | جميع الشاشات RTL، قابلة للتمرير، ولا يوجد تداخل أو شاشة بيضاء |
| الصلاحيات | القوائم والعمليات تتبع الصلاحيات بعد إعادة تسجيل الدخول |
| البيانات | لا تُحفظ معرفات أو مبالغ أو تواريخ غير صالحة |
| التقارير | لا توجد أسماء أعمدة قديمة أو `SELECT *` في تقارير الرسوم والدرجات الحالية |
| التدقيق | التغييرات الحساسة تظهر باسم المستخدم والكيان والمعرف |
| النسخ الاحتياطي | إنشاء واستعادة آمنان مع استرجاع حالة قاعدة البيانات |
| الاستقرار | لا توجد استثناءات غير معالجة أثناء السيناريوهات السلبية |

## طريقة إرسال نتائج الاختبار

عند ظهور خطأ، أرسل اسم الشاشة، اسم المستخدم والصلاحية، خطوات إعادة الخطأ، نص رسالة الخطأ كاملاً، ولقطة شاشة إن أمكن. أرفق كذلك رقم commit الحالي من الأمر التالي:

```powershell
git rev-parse --short HEAD
```

النسخة التي أُنجزت عليها هذه القائمة هي `bceed8b`، والفرع المحلي والبعيد `main` متزامنان عند آخر تحديث.

## ملاحظات إنتاجية

لا يُنصح بإعلان النظام جاهزاً للإنتاج قبل نجاح اختبار الاستعادة على نسخة اختبارية من SQL Server، واختبار الحسابات المحدودة، وإتمام Rebuild من Visual Studio على جهاز Windows الفعلي. التدقيق الساكن مؤشر جودة مهم، لكنه لا يستبدل اختبار WinForms وSQL Server الحقيقيين.
