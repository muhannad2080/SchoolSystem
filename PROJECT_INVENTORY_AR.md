# خريطة مشروع SchoolSystem — نقطة البداية

## الحالة المحلية

الفرع الحالي هو `main`، والنسخة المحلية متقدمة عن `origin/main` بcommit واحد هو `f3c7cce`، وهو تقرير الجاهزية العربي الذي لم يُرفع بعد بسبب انتهاء صلاحية مصادقة GitHub. لا توجد تغييرات غير محفوظة في working tree.

## البنية العامة

المشروع الحالي هو حل Visual Studio باسم `SchoolSystem.sln` يحتوي على مشروع `SchoolSystem.csproj` وملف إعداد `App.config`. البنية الحالية تحافظ على نمط الطبقات الموجود بدلاً من إنشاء مشروع جديد.

| الطبقة | العدد التقريبي | الملاحظات |
|---|---:|---|
| واجهات UI | 50 ملف C# | WinForms/UserControls مع دعم RTL وتصميم موحد جزئياً. |
| Services | 30 ملف C# | منطق الأعمال والتنسيق بين الواجهات والمستودعات. |
| DataAccess/Repositories | 28 ملف C# | الوصول إلى SQL Server والاستعلامات والمعاملات. |
| Models | 26 ملف C# | نماذج الطلاب والمعلمين والدرجات والمالية وغيرها. |
| سكربتات SQL | 10 ملفات | تتضمن الإنشاء والإضافات وسكربت سلامة البيانات للقراءة فقط. |

## نتائج الفحص الأولي

لم يظهر أي `TODO` أو `FIXME` أو `NotImplementedException` ضمن ملفات C# وSQL التي فُحصت. كما لم تظهر استعلامات SQL أو كائنات `SqlConnection` مباشرة داخل ملفات UI، وهو ما يتوافق مع الحفاظ على فصل الواجهات عن طبقة الوصول للبيانات.

تم تنفيذ التدقيق الساكن في الدفعات السابقة بنجاح، لكن بيئة التدقيق لا تحتوي على Visual Studio/MSBuild، ولذلك يلزم تنفيذ Build فعلي على Windows قبل اعتماد الإنتاج.

## الأولويات التالية

الأولوية الأولى هي مطابقة `App.config` وملف المشروع وسكربتات قاعدة البيانات مع المستودعات والخدمات والنماذج، ثم تحديد أي اختلاف في أسماء الأعمدة أو أنواع البيانات أو المفاتيح الأجنبية. بعد ذلك تُنفّذ إصلاحات صغيرة مستقلة، ويُشغّل التدقيق الساكن، ثم يُنشأ commit منفصل ويُرفع إلى `main` قبل الانتقال إلى الدفعة التالية.

## قاعدة التنفيذ

لا يُنشأ مشروع جديد، ولا تُحذف جداول أو بيانات، ولا تتصل الواجهات مباشرة بقاعدة البيانات. أي تعديل مخطط يجب أن يكون آمناً وقابلاً لإعادة التشغيل ويتحقق من وجود العنصر قبل الإضافة أو التعديل.
