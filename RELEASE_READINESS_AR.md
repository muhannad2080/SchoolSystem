# تقرير جاهزية الإصدار — SchoolSystem

## الحالة الحالية

الإصدار الحالي **جاهز للانتقال إلى اختبار قبول تجريبي**، وليس من المسؤول إعلان اعتماده إنتاجيًا قبل تنفيذ اختبارات Windows وSQL Server الفعلية. تم رفع آخر التعديلات إلى فرع `main` بعد نجاح الفحص الثابت والبناء داخل بيئة Linux باستخدام `xbuild`.

| المجال | الحالة | الدليل أو الإجراء التالي |
|---|---|---|
| البناء والتجميع | مكتمل | `xbuild SchoolSystem.sln` ينجح دون أخطاء تجميع |
| الفحص الثابت | مكتمل | `tools/validate_project.py` ينجح |
| إعداد الاتصال | مكتمل | مصدر مركزي في `DbConnection` وقالب `App.config.example` |
| التسجيل ومعالجة الأخطاء | مكتمل | `ApplicationLogger` و`UIHelper.ShowException` |
| الصلاحيات | مكتمل جزئيًا ومطبق على الخدمات الأساسية | اختبار الأدوار فعليًا على قاعدة تجريبية |
| تجربة التنقل | محسنة | اختبار بصري على Windows والتنقل بين جميع الشاشات |
| النسخ الاحتياطي | الأدوات والتوثيق مكتملة | تشغيل `scripts/Backup-SchoolDB.ps1` على SQL Server تجريبي |
| الاستعادة | السكربت مكتمل | استعادة إلى قاعدة اختبار وفحص عدد السجلات والتقارير |
| التقارير والطباعة | لم تُعتمد بيئيًا | اختبار الطابعات وملفات PDF/Excel على Windows |
| الأداء مع بيانات فعلية | لم يُقَس | اختبار ببيانات بحجم المدرسة ومراقبة زمن التحميل |

## بوابة القبول الإلزامية

لا يُنقل الإصدار إلى الاستخدام الفعلي إلا بعد نجاح تسجيل الدخول، وتوزيع الصلاحيات حسب الأدوار، وإضافة وتعديل وحذف سجل تجريبي في كل وحدة، وتشغيل التقارير والتصدير والطباعة، ثم تنفيذ نسخة احتياطية واستعادتها إلى قاعدة منفصلة. يجب تسجيل نتيجة كل اختبار واسم المنفذ والتاريخ.

## ملاحظات بيئية

توجد تحذيرات بناء غير مانعة بسبب استخدام أداة Linux لبناء مشروع Windows Forms يستهدف .NET Framework 4.7.2 وبعض مراجع NuGet الخاصة بالتقارير. يجب إجراء البناء النهائي من Visual Studio أو MSBuild على Windows، ثم اختبار الملف التنفيذي الناتج على جهاز تشغيل نظيف.

## قرار الإصدار

**قرار هذه المرحلة: مرشح لاختبار القبول التجريبي.** لا توجد أخطاء تجميع أو تغييرات غير مرفوعة في المستودع، لكن اعتماد الإنتاج يتطلب تحققًا يدويًا على بيئة المدرسة الفعلية، خصوصًا اتصال SQL Server، التقارير، الطباعة، النسخ الاحتياطي والاستعادة.
